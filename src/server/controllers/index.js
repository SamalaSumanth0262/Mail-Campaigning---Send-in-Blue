//all controllers are inherited from here
const authController = require('./authController');
const sendInBlueController = require('./sendInBlueController');
module.exports = {
  authController,
  sendInBlueController
};
